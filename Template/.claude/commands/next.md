# 次の機能に着手

以下の手順で次の機能の実装を開始してください：

1. `.claude/features.json` から次の "failing" 機能を特定
2. 依存関係が満たされているか確認
3. 機能の実装を開始
4. 完了したら:
   - Gitコミット（メッセージに機能IDを含める）
   - `features.json` の status を "passing" に更新
   - `claude-progress.txt` を更新

## 注意事項

- 一度に1つの機能のみ
- 大きな機能は小さなステップに分割
- 各ステップでコミット
